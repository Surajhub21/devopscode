apiVersion: v1
kind: Service

metadata:
  name: service-demo-db-app

spec:
  selector:
    app: demo-db-app
  ports:
    - name: http
      port: 8081 #map to our system port
      targetPort: 8081 #kubernetes pod port
  type: LoadBalancer
  